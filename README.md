# fastsearch

A simple and multithreaded file search utility written in C, fastsearch is designed for user and script usage.
It creates the maximum possible number of system threads that it can use for efficiency.
It returns one found location of the filename per line.
Filename comparison is exact-match only.
